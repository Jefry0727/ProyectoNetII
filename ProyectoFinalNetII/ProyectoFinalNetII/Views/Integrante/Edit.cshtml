@model Platform.Entity.Entity.Integrante

@{
    ViewBag.Title = "Edit";
}

<h2>Editar</h2>


@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal">
        <h4>Integrante</h4>
        <hr />
        @Html.ValidationSummary(true)
        @Html.HiddenFor(model => model.id)

        <div class="form-group">
            @Html.LabelFor(model => model.Proyecto_id, "Proyecto", new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.DropDownList("Proyecto_id", "Seleccione..")
                @Html.ValidationMessageFor(model => model.Proyecto_id)
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.Cargo_id, "Cargo", new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.DropDownList("Cargo_id", "Seleccione..")
                @Html.ValidationMessageFor(model => model.Cargo_id)
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.Usuario_id, "Usuario", new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.DropDownList("Usuario_id", "Seleccione..")
                @Html.ValidationMessageFor(model => model.Usuario_id)
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Editar" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Volver", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
